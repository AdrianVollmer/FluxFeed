# Import Feeds

On the feeds page, add a button: "Import feeds".

It should open a modal, where the user can paste a list of feeds, one feed per
line. The line should contain the URL, and then, separated by a space, an
optional description.
