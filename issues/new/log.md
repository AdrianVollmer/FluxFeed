# User facing Logs

Add a `Logs` page that shows various interesting events. In particular,
it should show when a feed was fetched. If a feed returned "429 Too Many
Requests", that event should have highest priority, like an error, and
displayed using red color.
