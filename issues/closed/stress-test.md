Create a python script that sets up a DB in `/tmp` and populates it with at
least 100k articles and at least 1k feeds (with an invalid URL).

Randomize contents using lorem ipsum. Insert counters in titles etc.

Assign random tags to feeds.
