# Fetch now indicator

When clicking on "fetch now" in the feed detail view, show some sort of
indicator to give the user some feedback that the fetch process has been
started.

Same after adding a feed. Even fetching the feed just one can introduce
a noticable delay, for example when the network is down. The feed list
should show this feed greyed out as "Initializing ..."
