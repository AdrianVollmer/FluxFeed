# Favicon

Add a favicon as a SVG. It should contain the two letters 'FF'. In the
background should be something that flows, like a wave or something like that.
The colors should match our primary palette.
